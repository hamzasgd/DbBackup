{
  "name": "dbbackup",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace dbbackup-server dev\" \"yarn workspace client dev\"",
    "build": "yarn workspace dbbackup-server build && yarn workspace client build",
    "install:all": "yarn install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "packageManager": "yarn@1.22.22"
}
